<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!--网络权限-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name=".Startup"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme_Auto_Script"
        tools:targetApi="31">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MacroListActivity" />
        <activity android:name=".activities.DisclaimerActivity" />
        <activity android:name=".activities.ActionAddActivity" />
        <activity android:name=".activities.PointAddActivity" />
        <activity android:name=".activities.MacroInfoActivity" />
        <activity android:name=".activities.SettingActivity" />
        <!--关于界面-->
        <activity android:name=".activities.AboutActivity" />
        <activity android:name=".activities.about.DefaultEmptyAboutActivity" />
        <activity android:name=".activities.about.ErrorAboutActivity" />
        <!--关于子界面-->
        <activity android:name=".activities.about.child.AboutBasicInfoActivity" />
        <activity android:name=".activities.about.child.AboutAcknowledgmentsActivity" />
        <activity android:name=".activities.about.child.AboutAdsActivity" />
        <activity android:name=".activities.about.child.AboutLicensesActivity" />
        <activity android:name=".activities.about.child.AboutUpdateLogActivity" />
        <activity android:name=".activities.about.child.AboutDevelopmentProcessActivity" />
        <activity android:name=".activities.about.child.AboutFAQActivity" />
        <activity android:name=".activities.about.child.AboutTroubleshootingActivity" />

        <service
            android:name=".service.MyAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>